{
  "name": "mcp-br-commons",
  "private": true,
  "description": "Componentes compartilhados do portfólio de servidores MCP (Fase 0)",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/*",
    "templates/*"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "vitest": "^5.0.0"
  }
}
